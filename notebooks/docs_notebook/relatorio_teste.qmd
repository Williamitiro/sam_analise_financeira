---
title: "📊 Executive Cockpit - Projeção Financeira 36 Meses"
subtitle: "Relatório de Análise SAM"
author: "Gerado Automaticamente"
date: today
format:
  pdf:
    toc: true
    toc-depth: 2
    number-sections: true
    colorlinks: true
    papersize: a4
    geometry:
      - margin=2cm
    fontsize: 11pt
    include-in-header:
      text: |
        \usepackage{graphicx}
        \usepackage{float}
execute:
  echo: false
  warning: false
jupyter: python3
---

# Visão Executiva

Este relatório apresenta a projeção financeira para os próximos 36 meses, comparando o cenário **Real** (bootstrap) com o cenário **Ideal** (benchmark da indústria).

## KPIs Estratégicos

```{python}
#| label: fig-kpi-cards
#| fig-cap: "Painel de KPIs Estratégicos - Métricas M36"

from IPython.display import Image, display
import os

# Verificar se as imagens existem
img_path = 'outputs/figs/pg1_kpi_cards.png'
if os.path.exists(img_path):
    display(Image(filename=img_path, width=700))
else:
    print("⚠️ Execute o notebook primeiro para gerar os gráficos")
```

# Evolução MRR vs Usuários

O gráfico abaixo mostra a evolução da Receita Recorrente Mensal (MRR) comparada com o crescimento de usuários ativos ao longo de 36 meses.

```{python}
#| label: fig-temporal
#| fig-cap: "Evolução MRR vs Usuários - Cenário Real vs Ideal"

img_path = 'outputs/figs/pg1_temporal_correlacao.png'
if os.path.exists(img_path):
    display(Image(filename=img_path, width=700))
else:
    print("⚠️ Execute o notebook primeiro para gerar os gráficos")
```

# Eficiência de Marketing

Análise da relação entre investimento em marketing e receita gerada (MRR).

**Importante:** A métrica "Eficiência" aqui representa `MRR / Gasto_Marketing`, que é diferente de LTV/CAC.

```{python}
#| label: fig-eficiencia
#| fig-cap: "Investimento em Marketing vs Receita Gerada (MRR)"

img_path = 'outputs/figs/pg1_eficiencia_marketing.png'
if os.path.exists(img_path):
    display(Image(filename=img_path, width=700))
else:
    print("⚠️ Execute o notebook primeiro para gerar os gráficos")
```

# Conclusões

Este relatório foi gerado automaticamente a partir dos dados do notebook `real_vs_ideal.ipynb`.

Para atualizar, execute novamente o código de geração de gráficos e depois renderize este documento com:

```bash
quarto render relatorio_teste.qmd
```
